// components/menu/MenuScreen.styles.js
import {StyleSheet} from 'react-native';
import {SIZES} from '../../../constants';

export default StyleSheet.create({
  container: {
    padding: SIZES.medium,
  },
});
